<h3> <%= @book.name %></h3>

<p><%= link_to "Delete", book_recipe_path(@book,@recipe), method: :delete, data: { confirm: 'Are you sure?' }%></p>
  <%= link_to "Edit", edit_book_recipe_path(@book,@recipe)%>
	<img src="<%= @recipe.img_url %>" alt="" />
  <h3> <%= @recipe.title %></h3>
  <p><%= @recipe.ingredients %></p>
  <p><%= @recipe.steps %></p>
